# RoleFit Canvas 환경 변수
#
# 이 앱은 키가 없어도 전부 동작합니다.
# 규칙 기반 분석 엔진이 기본이고, 아래 키가 있으면 정밀 분석으로 전환됩니다.

# Anthropic API 키 (선택). 설정하면 JD·이력 분석과 매칭 설명이 LLM 기반으로 바뀝니다.
# 서버에서만 읽습니다. 클라이언트로 내려가지 않습니다.
ANTHROPIC_API_KEY=
